// Generated by CoffeeScript 1.4.0
(function() {
  var S, inspect, isArray, isString, readString;

  inspect = require('util').inspect;

  isArray = function(obj) {
    return Array.isArray(obj);
  };

  isString = function(obj) {
    return !!(obj === '' || (obj && obj.charCodeAt && obj.substr));
  };

  readString = function(p) {
    return p.replace(/\s+/g, ', ').replace(/\)\(/g, '), (').replace(/\(/g, '[').replace(/\)/g, ']').replace(new RegExp("([a-zA-Z][a-zA-Z0-9]*)", "gi"), "\"$1\"");
  };

  module.exports = S = function(p) {
    var e, s;
    if (isString(p)) {
      return eval(readString(p));
    } else {
      if (isArray(p)) {
        s = (function() {
          var _i, _len, _results;
          _results = [];
          for (_i = 0, _len = p.length; _i < _len; _i++) {
            e = p[_i];
            if (isArray(e)) {
              _results.push(S(e));
            } else {
              _results.push("" + e);
            }
          }
          return _results;
        })();
        return "(" + (s.join(' ')) + ")";
      }
    }
  };

}).call(this);
