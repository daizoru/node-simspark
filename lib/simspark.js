// Generated by CoffeeScript 1.6.3
(function() {
  var Stream, buffy, checkServer, checkViewer, execSync, inspect, isArray, isString, net, pretty, sexp, spawn, startServer, startViewer,
    __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  inspect = require('util').inspect;

  net = require('net');

  Stream = require('stream');

  spawn = require('child_process').spawn;

  execSync = require('execSync');

  buffy = require('buffy');

  sexp = require('./s-expression');

  pretty = function(obj) {
    return "" + (inspect(obj, false, 20, true));
  };

  isArray = function(obj) {
    return Array.isArray(obj);
  };

  isString = function(obj) {
    return !!(obj === '' || (obj && obj.charCodeAt && obj.substr));
  };

  exports.Client = (function(_super) {
    __extends(Client, _super);

    function Client(options) {
      var headerLen, length, _ref, _ref1,
        _this = this;
      if (options == null) {
        options = {};
      }
      Client.__super__.constructor.call(this, this);
      this.host = (_ref = options.host) != null ? _ref : 'localhost';
      this.port = (_ref1 = options.port) != null ? _ref1 : 3100;
      this.client = net.connect(this.port, this.host);
      this.client.on('error', function(er) {
        return _this.emit('error', er);
      });
      this.client.on('connect', function(er) {
        return _this.emit("connect", function() {});
      });
      this.client.on('close', function() {
        return _this.emit('close');
      });
      this.reader = buffy.createReader();
      this.client.pipe(this.reader);
      headerLen = 4;
      length = headerLen;
      this.client.on('data', function(data) {
        var er, evt, message, rawMsg, _results;
        _results = [];
        while (_this.reader.bytesAhead() >= length) {
          if (length === headerLen && _this.reader.bytesAhead() >= headerLen) {
            length = _this.reader.int32BE();
          }
          if (_this.reader.bytesAhead() >= length) {
            rawMsg = _this.reader.ascii(length);
            message = [];
            try {
              message = sexp("(" + rawMsg + ")");
            } catch (_error) {
              er = _error;
              console.log("couldn't parse: " + pretty(er));
              console.log("original: " + pretty("(" + rawMsg + ")"));
            }
            length = headerLen;
          }
          _results.push((function() {
            var _i, _len, _results1;
            _results1 = [];
            for (_i = 0, _len = message.length; _i < _len; _i++) {
              evt = message[_i];
              _results1.push(this.emit(evt[0].toLowerCase(), evt.slice(1)));
            }
            return _results1;
          }).call(_this));
        }
        return _results;
      });
    }

    Client.prototype.send = function(messages) {
      var msg, msgPacket, msgString, _i, _len, _results;
      if (messages == null) {
        messages = [];
      }
      _results = [];
      for (_i = 0, _len = messages.length; _i < _len; _i++) {
        msg = messages[_i];
        msgString = sexp(msg);
        msgPacket = new Buffer(4 + msgString.length);
        msgPacket.writeInt32BE(msgString.length, 0);
        msgPacket.write(msgString, 4, 'ascii');
        _results.push(this.client.write(msgPacket));
      }
      return _results;
    };

    Client.prototype.close = function() {
      var er, _ref;
      try {
        return (_ref = this.client) != null ? typeof _ref.destroy === "function" ? _ref.destroy() : void 0 : void 0;
      } catch (_error) {
        er = _error;
        return console.log("notice: couldn't destroy the socket");
      }
    };

    return Client;

  })(Stream);

  exports.Agent = (function(_super) {
    __extends(Agent, _super);

    function Agent(options) {
      if (options == null) {
        options = {};
      }
      Agent.__super__.constructor.call(this, {
        port: 3100
      });
    }

    return Agent;

  })(exports.Client);

  exports.Monitor = (function(_super) {
    __extends(Monitor, _super);

    function Monitor(options) {
      if (options == null) {
        options = {};
      }
      Monitor.__super__.constructor.call(this, {
        port: 3200
      });
    }

    return Monitor;

  })(exports.Client);

  exports.startServer = startServer = function(onReady) {
    var isReady, server;
    server = spawn("rcssserver3d", []);
    isReady = false;
    server.stdout.on('data', function(data) {
      if (!isReady) {
        isReady = true;
        return onReady(server);
      }
    });
    server.stderr.on('data', function(data) {});
    server.on('close', function(code, signal) {
      return log("rcssserver3D: exited");
    });
    return server.stdin.end();
  };

  exports.startViewer = startViewer = function(onReady) {
    var isReady, viewer;
    viewer = spawn("rcssmonitor3d", []);
    isReady = false;
    viewer.stdout.on('data', function(data) {
      if (!isReady) {
        isReady = true;
        return onReady(viewer);
      }
    });
    viewer.stderr.on('data', function(data) {});
    viewer.on('close', function(code, signal) {
      return log("rcssmonitor3D: exited");
    });
    return viewer.stdin.end();
  };

  exports.checkServer = checkServer = function(cb) {
    var instances, psaux;
    console.log("Checking if Simspark Server is running..");
    psaux = execSync.exec('ps aux | grep rcssserver3d | grep -v grep | wc -l; exit 1');
    instances = Number(psaux.stdout);
    if (instances === 0) {
      console.log("Server is not running, starting it..");
      return startServer(function(instance) {
        log("Server is now started, connecting to it..");
        return cb();
      });
    } else {
      console.log("Server is already running, connecting to it..");
      return cb();
    }
  };

  exports.checkViewer = checkViewer = function(cb) {
    var instances, psaux;
    console.log("Checking if Simspark Viewer is running..");
    psaux = execSync.exec('ps aux | grep rcssmonitor3d | grep -v grep | wc -l; exit 1');
    instances = Number(psaux.stdout);
    if (instances === 0) {
      log("Viewer is not running, starting it..");
      return startViewer(function(instance) {
        console.log("Viewer is now started, connecting to it..");
        return cb();
      });
    } else {
      console.log("Viewer is already running, connecting to it..");
      return cb();
    }
  };

}).call(this);
